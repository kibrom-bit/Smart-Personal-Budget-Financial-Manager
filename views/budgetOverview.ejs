<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Budget Overview</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }

        .container {
            max-width: 900px;
            background-color: white;
            padding: 30px;
            margin-top: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .overview {
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #007bff;
            color: white;
        }

        .total-expenses {
            font-size: 1.2rem;
            font-weight: bold;
            margin-top: 20px;
        }

        .btn-link {
            margin-top: 20px;
            display: inline-block;
            font-weight: bold;
            color: #007bff;
        }

        .btn-link:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>

    <div class="container">
        <h1 class="text-center mb-5">Budget Overview for <%= year %> - <%= month %> (የ<%= year %>ዓ.ም <%= month %> ወር ባጀት መጠን)</h1>

        <div class="overview">
            <h2>Budget</h2>
            <p><strong>Budget(ባጀት):</strong> <%= budget.budget_amount %></p>
        </div>

        <div class="overview">
            <h2>Expenses(ወጪዎች)</h2>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Category(ምድብ)</th>
                        <th>Amount(መጠን)</th>
                        <th>Reason(ምክንያት)</th>
                        <th>Date(ቀን</th>
                    </tr>
                </thead>
                <tbody>
                    <% expenses.forEach(expense => { %>
                        <tr>
                            <td><%= expense.category %></td>
                            <td><%= expense.amount %></td>
                            <td><%= expense.reason %></td>
                            <td><%= expense.year %>-<%= expense.month %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>

        <div class="total-expenses">
            <h3>Total Expenses: <%= totalExpenses %></h3>
        </div>

        <form action="/select-year-month">
            <button type="submit" class="btn btn-secondary btn-block mt-3">Go to Dashboard</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
