<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Optional: Add custom styles -->
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }

        .profile-section {
            background-color: white;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .profile-section h2 {
            margin-bottom: 20px;
        }

        .profile-section form label {
            font-weight: bold;
        }

        .profile-section form input {
            width: 100%;
            padding: 10px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .profile-section form button {
            padding: 10px 15px;
            border: none;
            background-color: #007bff;
            color: white;
            border-radius: 4px;
            cursor: pointer;
        }

        .profile-section form button:hover {
            background-color: #0056b3;
        }

        .profile-section p {
            font-size: 1.1rem;
        }

        a {
            display: inline-block;
            margin-top: 20px;
            font-weight: bold;
            color: #007bff;
        }

        a:hover {
            text-decoration: underline;
        }

        .alert {
            margin-bottom: 20px;
        }
    </style>
</head>

<body>

    <div class="container my-4">
        <h1 class="text-center mb-5">Your Profile(የእርስዎን መገለጫ)</h1>

        <!-- Display Error or Success Messages -->
        <% if (errorMessage) { %>
            <div class="alert alert-danger">
                <%= errorMessage %>
            </div>
        <% } %>
        <% if (successMessage) { %>
            <div class="alert alert-success">
                <%= successMessage %>
            </div>
        <% } %>

        <!-- Profile Details Section -->
        <div class="profile-section">
            <h2>Your Profile(የእርስዎን መገለጫ)</h2>
            <p><strong>Username(የተጠቃሚ ስም):</strong> <%= user.username %></p>
            <p><strong>Email(ኢሜይል):</strong> <%= user.email %></p>
        </div>

        <!-- Update Profile Section -->
        <div class="profile-section">
            <h2>Update Profile(መገለጫ ያዘምኑ)</h2>
            <form action="/profile/update" method="POST">
                <div class="mb-3">
                    <label for="email" class="form-label">Email(ኢሜይል):</label>
                    <input type="email" id="email" name="email" value="<%= user.email %>" required class="form-control">
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">New Password(አዲስ የይለፍ ቃል):</label>
                    <input type="password" id="password" name="password" class="form-control">
                </div>
                <button type="submit" class="btn btn-primary">Submit(አስገባ)</button>
            </form>
        </div>

        <!-- Navigation Links -->
        <div class="text-center">
            <form action="/select-year-month">
                <button type="submit" class="btn btn-primary">Go Back(ወደ ኃላ)</button>
            </form>
            <a href="users/logout" class="btn btn-danger">Logout(ዘግቶው ይመለሱ)</a>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>
