<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expenses Overview for <%= month %> <%= year %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }

        .container {
            max-width: 900px;
            background-color: white;
            padding: 30px;
            margin-top: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th,
        td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #007bff;
            color: white;
        }

        a {
            color: #007bff;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        .btn-link {
            margin-top: 20px;
            display: inline-block;
            font-weight: bold;
            color: #007bff;
        }

        .btn-link:hover {
            text-decoration: underline;
        }

        .no-records {
            font-size: 1.2rem;
            font-weight: bold;
        }
    </style>
</head>

<body>

    <div class="container">
        <h1 class="text-center mb-5">Expenses Overview for <%= month %> <%= year %>(የ<%= year %>ዓ.ም <%= month %> ወር ወጪዎች)</h1>
        
        <% if (expenses.length > 0) { %>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Category(ምድብ)</th>
                        <th>Amount(መጠን)</th>
                        <th>Reason(ምክንያት)</th>
                        <th>Action(ድርጊት)</th>
                    </tr>
                </thead>
                <tbody>
                    <% expenses.forEach(expense => { %>
                        <tr>
                            <td><%= expense.category %></td>
                            <td><%= expense.amount %></td>
                            <td><%= expense.reason %></td>
                            <td>
                                <a href="/expenses/update/<%= expense.id %>/<%=month %>/<%=year %>" class="btn btn-sm btn-primary">Edit(ያስተካክሉ)</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } else { %>
            <p class="no-records">No expenses recorded for this month(ይህን ወር የሚታወቅ ወጪ አልተገኘም).</p>
        <% } %>

        <form action="/select-year-month">
            <button type="submit" class="btn btn-secondary btn-block mt-3">Go Back(ወደ ኃላ)</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
