<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Savings Goal</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      font-family: 'Arial', sans-serif;
    } 
    h1 {
      background-color:white;
      color: #007bff;
    }

    header {
      text-align: center;
      margin-bottom: 20px;
    }

    footer {
      text-align: center;
      margin-top: 20px;
    }

    .container {
      max-width: 600px;
      background-color: white;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .form-label {
      font-weight: bold;
    }

    .form-control {
      margin-bottom: 15px;
    }

    button {
      width: 100%;
    }
  </style>
</head>

<body>

  <div class="container mt-5">
    <header>
      <h1>Add Savings Goal(የዚህ ወር የቁጠባ ግብዎትን ያስገቡ)</h1>
    </header>

    <!-- Display error or success message -->
    <div id="message">
      <!-- Success message -->
      <% if (successMessage) { %>
        <div class="alert alert-success" role="alert">
          <%= successMessage %>
        </div>
      <% } %>

      <!-- Error message -->
      <% if (errorMessage) { %>
        <div class="alert alert-danger" role="alert">
          <%= errorMessage %>
        </div>
      <% } %>
    </div>

    <form action="/savings/add/<%=month %>/<%=year %>" method="POST">
      <div class="mb-3">
        <label for="savingsGoal" class="form-label">Savings Goal(የቁጠባ ግብ በብር)</label>
        <input type="number" class="form-control" name="savingsGoal" id="savingsGoal" placeholder="ለምሳሌ. 500.00" required>
      </div>

      <div class="mb-3">
        <label for="month" class="form-label">Month(ወር)</label>
        <input type="number" class="form-control" name="month" id="month" value="<%=month %>" min="1" max="12" readonly required>
      </div>

      <div class="mb-3">
        <label for="year" class="form-label">Year(ዓመት)</label>
        <input type="number" class="form-control" name="year" id="year" value="<%=year %>" min="1" max="12" readonly required>
      </div>

      <button type="submit" class="btn btn-primary">Submit(አስገባ)</button>
    </form>

    <footer>
      <form action="/select-year-month">
        <button type="submit" class="btn btn-secondary btn-block mt-3">Go Back(ወደ ኃላ)</button>
    </form>
    </footer>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
